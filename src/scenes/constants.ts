// Ground dimensions
export const GROUND_HEIGHT = 10
export const GROUND_WIDTH = 10;

// Ground boundaries
export const MIN_X = -GROUND_WIDTH / 2;
export const MAX_X = GROUND_WIDTH / 2;
export const MIN_Z = -GROUND_HEIGHT / 2;
export const MAX_Z = GROUND_HEIGHT / 2;